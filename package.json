{
  "name": "organicgrowth",
  "version": "0.1.0",
  "private": true,
  "description": "OrganicGrowth Producer feature code: harness, deep modules, and plain-file state.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "tsc -p tsconfig.json --noEmit && node --import tsx --test \"src/**/*.test.ts\"",
    "queue": "tsx src/commands/queue.ts",
    "pick-cast": "tsx src/commands/pick-cast.ts"
  },
  "dependencies": {
    "yaml": "^2.9.0"
  },
  "devDependencies": {
    "@fission-ai/openspec": "^1.3.0",
    "@types/node": "^20.19.0",
    "tsx": "^4.22.0",
    "typescript": "^5.9.0"
  }
}
