{
  "safety-gate": {
    "PreToolUse": [
      {
        "matcher": "mcp__zoho.*publish.*|.*publishSocialPost.*",
        "hooks": [
          {
            "type": "command",
            "command": "echo '{\"decision\": \"deny\", \"reason\": \"Rule 1: Generate-never-publish. OrganicGrowth assets must be published by the human operator.\"}'"
          }
        ]
      }
    ]
  }
}
